# Optional: SML/C++ binding notes

Not a second standard. Use only to phrase remediation in stateforward.SML / emel terms after a core finding.

Examples: guards in transition table / `guards.hpp`, no runtime branch in `actions`/`detail`, do-work in activity-equivalent phases, completion as events not action-local if/else path choice.
