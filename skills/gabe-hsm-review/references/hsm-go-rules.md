# Optional: hsm.go binding notes

Not a second standard. Use only to phrase remediation in Go/hsm.go terms after a core (UML-agnostic) finding.

Examples: choice → `hsm.Choice`, time → `hsm.After`/`Every`/`At`, activity → `hsm.Activity`, defer → `hsm.Defer`.
